<%= form_with(model: like, url: "/likes", method: :post) do |form| %>
  <% if like.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
        <% like.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id, value: current_user.id %>
  </div>

  <div>
    <%= form.label :tweet_id, style: "display: block" %>
    <%= form.text_field :tweet_id, value: like.tweet.id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
